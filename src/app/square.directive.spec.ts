import { SquareDirective } from './square.directive';

describe('SquareDirective', () => {
  it('should create an instance', () => {
    const directive = new SquareDirective();
    expect(directive).toBeTruthy();
  });
});

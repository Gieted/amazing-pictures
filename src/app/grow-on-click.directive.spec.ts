import { GrowOnClickDirective } from './grow-on-click.directive';

describe('GrowOnClickDirective', () => {
  it('should create an instance', () => {
    const directive = new GrowOnClickDirective();
    expect(directive).toBeTruthy();
  });
});

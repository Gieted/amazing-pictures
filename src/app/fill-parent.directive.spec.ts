import { FillParentDirective } from './fill-parent.directive';

describe('FillParentDirective', () => {
  it('should create an instance', () => {
    const directive = new FillParentDirective();
    expect(directive).toBeTruthy();
  });
});
